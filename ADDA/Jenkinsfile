pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Build') {
            steps {
                script {
                    def mvnHome = tool 'Maven'
                    sh "C:/Program Files/apache-maven-3.9.5/bin/mvn clean install"
                }
            }
        }
    }

    post {
        failure {
            echo "Build failed! See above for details."
        }
    }
}
